name: CI - Build script in a Docker Image

on:
  pull_request:
    paths-ignore:
      - '**.md'
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2
      with:
        fetch-depth: 1

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v1

    - name: Set up Docker buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUBTOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        file: ./.dockerdev/Dockerfile
        tags: ghcr.io/${{ github.repository_owner }}/dotfiles:latest
        push: true

    - name: Image digest
      run: echo ${{ steps.docker_build.outputs.digest }}

      # -
      #   name: Build and push
      #   id: docker_build
      #   uses: docker/build-push-action@v2
      #   with:
      #     context: .
      #     file: ./.dockerdev/Dockerfile
      #     tags: luciotbc/dotfiles:latest
      #     push: true
      #     secrets: |
      #       GIT_AUTH_TOKEN=${{ secrets.GITHUBTOKEN }}
      # -

# jobs:
#   build:
#     runs-on: ubuntu-20.04
#     steps:
#       -
#         name: Checkout
#         uses: actions/checkout@v2
#         with:
#           fetch-depth: 1
#       - 
#         name: Build and Publish Tag Docker image
#         uses: VaultVulp/gp-docker-action@1.1.7
#         with:
#           github-token: ${{ secrets.GITHUBTOKEN }}
#           image-name: dotfiles
#           build-context: .dockerdev
#           extract-git-tag: true # Provide flag to extract Docker image tag from git reference

      # -
      #   name: Build
      #   run: |
      #     docker build .dockerdev/Dockerfile

      # -
      #   name: Install dotfiles
      #   run: |
      #     docker-compose run -u lucio base curl -sL https://raw.githubusercontent.com/luciotbc/dotfiles/lc-master/ci/_setup.sh | bash
